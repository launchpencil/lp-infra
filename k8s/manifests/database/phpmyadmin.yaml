apiVersion: apps/v1
kind: Deployment
metadata:
  name: phpmyadmin
  namespace: infra-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: database-web
  template:
    metadata:
      labels:
        app: database-web
    spec:
      containers:
      - name: phpmyadmin
        image: phpmyadmin/phpmyadmin
        env:
          - name: PMA_HOST
            value: "192.168.0.3"